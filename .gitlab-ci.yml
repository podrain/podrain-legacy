image: node:lts

cache:
  paths:
  - node_modules/

pages:
    script:
    - npm install -g yarn
    - yarn install
    - npx parcel build src/index.html --out-dir public

    artifacts:
        paths:
        - public
    only:
    - master
